/**
 * Move: activateAbility
 * Ativa habilidades secundárias de cartas com custo específico de energia.
 */
export function activateAbilityMove({ G, ctx, events }, actionPayload) {
  // TODO: Migrar a lógica do old-engine/resolveAbility.js
  console.log("Executando activateAbilityMove", actionPayload);
}
